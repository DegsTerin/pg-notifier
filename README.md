# pg-notifier
Professional PostgreSQL tray monitor for Windows, built with PowerShell. Monitors service status, checks connectivity with pg_isready, detects restarts by PID changes, supports multiple instances, notifications, logs, auto-discovery, and Windows installer packaging.
